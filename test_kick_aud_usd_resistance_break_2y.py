# 最新更新日時: 2026-09-04 JST
"""AUD/USD の抵抗線を 2023-07-30〜2025-07-30 の2年で検証する（手順4）。

## この走行の位置づけ

2025-07-30〜2026-07-30 の1年（第4〜7回走行）で見つけた条件が本物かを
確かめるための、独立した期間での検証。**1年側は既に in-sample。**

### 走行前に宣言する判定ルール（後から変えないこと）

> **上側の抵抗線は構成ピークの7割以上が高値、
> 下側の支持線は7割以上が安値。かつ peaks count >= 2。**

比率は `LineStrengthCal.native_direction_ratio` で計算する。
ピーク2個・3個で構成される線では7割は実質100%と同じになる
（1/2=0.5、2/3=0.67 はどちらも0.7に届かない）。これは承知のうえで採用する。
理由は「勝率が上がるから」ではなく、**安値だけで組まれた線を抵抗線と
呼ぶのは測る対象を取り違えているから**。閾値を成績で選ぶと偶然の最大値を拾う。

### 1年側で観測された値（これを確かめに行く）

3通貨プール、ブレイク側、損益分岐は RR1.2 なので 45.5%。

| 足 | 絞らない | 7割&peaks>=2 |
|---|---|---|
| M5 | 37.9% | 38.2%（平坦・効果なし） |
| M30 | 41.2% | **44.7%**（80%閾値では45.9%） |
| H1 | 39.0% | 40.0% |

**M30 だけに効果が出た。** M5 は標本10,211件・2SE=1.0pt で完全に平坦なので、
検出力不足ではなく本物の「無」。M30 の線の集団は4つの指標
（スキップ率・線の本数・強度分布・Aの水準）すべてで正常であり、
「壊れた集団だから効いて見える」という対抗仮説は否定済み。
残る対抗仮説は「3足試して1つ当たっただけの偶然」で、それをこの走行で判定する。

## なぜ走行では絞らないのか

`select_ahead_lines` は前方の線を上限なしで全部返すため、
**線を作る段階で落とすのと、候補CSVに後から条件をかけるのは結果が同じ**。
そこで走行は絞らずに記録し、上記のルールは解析時に適用する。
こうすると同じデータを後の探索にも使える。
ただし**報告するのは宣言したルールの結果だけ**とし、
他の条件（逆向き1個まで、10割、など）を試した場合は
検証ではなく探索として明示すること。

なお `candidate_rank` / `distance_rank` は絞る前の集合で振られるので、
順位を使う解析だけは走行時に絞った場合と一致しない。

## データ

M5・H1・S5 はキャッシュ済み。**native M30 は未取得なので、
実行時に OANDA から取得して保存される**（2年で約35,000本、数分）。

3通貨を並列で走らせるため通貨ごとにファイルを分けてある。
``win_point.PAIR`` へのモジュール変数の書き換えがあるので、
スレッドではなく必ずプロセスを分けること。

    python test_kick_aud_usd_resistance_break_2y.py
"""

from count2_resistance_sweep import main


PAIR = "AUD_USD"
ARGV = [
    # ★1年側（2025-07-30〜2026-07-30）とは重ならない独立した2年間
    "--start", "2023-07-30 00:00:00",
    "--end", "2025-07-30 00:00:00",
    # 以下は第7回走行と完全に同じ。ここを変えると比較にならない。
    "--entry-mode", "stop",
    "--stop-offset-pips", "1.0",
    "--stop-slippage-pips", "0.5",
    "--enforce-peak-strength",
    "--group-threshold-a", "0.5",
    "--target-grid",
    # 向きと peaks 数は絞らずに記録する（理由は上のdocstring）。
    # 解析時に line_positive_peak_count / line_negative_peak_count と
    # line_peaks_count から、宣言したルールを適用すること。
]


if __name__ == "__main__":
    main(PAIR, list(ARGV))
