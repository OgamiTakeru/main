# flip_predict 検討メモ・TODO

最終更新: 2026-08-26

## 次のタスク：USD_JPY をどうするか（未着手）

AUD_USD には「合致条件数3個以上」の重み付けが有効だったが、USD_JPY には効かない。
USD_JPY の OOS 実測：

| 方式 | n | win率 | PF | sum_yen |
|---|---|---|---|---|
| top-3 のみ | 15 | 53.3% | 1.26 | +61 |
| 合致2個以上 | 74 | 39.2% | 0.69 | -580 |
| 合致3個以上 | 15 | 53.3% | 1.22 | +57 |
| 現行（top15 OR） | 298 | 38.6% | 0.71 | -2250 |

**根本原因**：USD_JPY は合致1個のトレードが 224件（全体の75%）を占め、条件同士が重ならない。
合致3個以上は 15件しかなく、絞り込んでも重み付けしてもサンプルが枯渇する。

検討すべき方向：
- USD_JPY 固有の条件セットを別途探索する（現在の特徴量では上位が fc2 のヒゲ・押し戻し系に偏る）
- 条件同士が重ならない理由を調べる（条件が細分化されすぎ？ 母集団が広すぎ？）
- そもそも USD_JPY はこの戦略に向いていない可能性も検討対象
- AUD_USD と共通で効く条件が本当に無いのか、改めて確認

## 最優先の未解決課題：OR結合設計の見直し

**問題**：上位15条件を全部OR結合して「どれか1つでも合致したらエントリー」する現在の設計が、
勝てる条件の利益を、負ける条件の損失で打ち消してしまっている。

全機能適用後の OOS（2025-07-30〜2026-07-30）実測：

**AUD_USD**

| 対象 | n | win率 | PF | sum_yen |
|---|---|---|---|---|
| HIGH tier (rank1-5) | 48 | 56.2% | 1.49 | **+466** |
| MIDDLE tier (rank6-10) | 43 | 37.2% | 0.52 | -473 |
| LOW tier (rank11-15) | 19 | 36.8% | 0.64 | -150 |
| 全体（現行のtop15 OR） | 110 | 45.5% | 0.93 | **-157** |
| top-2 のみ | 33 | 60.6% | 2.02 | **+579** |

**USD_JPY**（AUD_USDほど改善せず。HIGH tierすら赤字）

| 対象 | n | win率 | PF | sum_yen |
|---|---|---|---|---|
| HIGH tier | 50 | 44.0% | 0.75 | -283 |
| MIDDLE tier | 163 | 35.6% | 0.71 | -1375 |
| LOW tier | 85 | 41.2% | 0.69 | -592 |
| 全体 | 298 | 38.6% | 0.71 | **-2250** |
| top-3 のみ | 15 | 53.3% | 1.26 | +61 |

共通するのは「上位2〜3件だけなら両ペアとも黒字、それ以下を足すと赤字に転落」。
OR結合の問題は2ペアで裏付けられた。

**検討すべき方向**：
- 採用条件数を15から減らす（HIGH tierのみ、top-5のみ、等）
- tierごとに採否を分ける（HIGHは採用、MIDDLE/LOWは見送り or 別ロジック）
- OR ではなく、複数条件に合致するほど信頼度を上げる重み付け設計
- ※ ただし「rank1だけ採用」のような極端な絞り込みは、サンプルが少なくなり
  かえって不安定になる恐れがあるため注意（ユーザー指摘済み）

## R単位の利益確保（RiskMultipleProfitLock）＝新規の試み

**これまでRRの下限を保証する仕組みは無かった。今回が初めての導入。**
（既存の `stretch_profit_lock` は「TPを2倍に伸ばす」別目的の機構で、しかも比較用の
副次計算にとどまっていた。本流のポリシーに組み込んだのは今回が初。）

**仕組み**：`RiskMultipleProfitLock(trigger_r=1.2, result_r=1.05)`
トレードが +1.2R に到達したら、ロスカを +1.05R の位置へ引き上げる。
以後そのトレードは +1.05R を下回って終われない。TPは変更しないので上値はそのまま。
消えるのは「一度伸びた勝ちトレードが反転して負けに終わる」テールだけ。

**tierごとのRR差を吸収する設計**：TP比率ではなくR単位で指定し、各tierのRRで割って
TP比率に変換する（`fractions_for_rr`）。RRが違っても実利益は必ず1.05Rになる。

| tier | RR | 発動(TP比) | 確保(TP比) | 実確保 |
|---|---|---|---|---|
| HIGH | 1.43 | 0.84 | 0.735 | 1.05R |
| MIDDLE | 1.77 | 0.68 | 0.594 | 1.05R |

**AUD_USD OOS 実測（効果は明確）**

| 設定 | n | 勝率 | PF | sum_yen |
|---|---|---|---|---|
| RR 1.07（合致3のみ） | 52 | 53.8% | 1.14 | +155 |
| RR 1.43（ロックなし） | 51 | 49.0% | 1.05 | +58 |
| **RR 1.43 + 1.2R→1.05R確保** | 51 | 52.9% | **1.25** | **+271** |

決済内訳の変化（ロックなし→あり）：timeout 17→13、LC 22→20、
代わりに profit_lock が 8件発生し +1.05R を確保。狙い通りの挙動。

**設定箇所**（すべて `count2_flip_core.py`、いつでも変更可）：

| 定数 | 現在値 | 意味 |
|---|---|---|
| `DEFAULT_MINIMUM_TIER_RR` | **1.4** | 全ペア共通のRR下限 |
| `PAIR_MINIMUM_TIER_RR` | `{}` | ペア個別の上書き。`0.0`を入れるとそのペアだけ無効 |
| `DEFAULT_RISK_MULTIPLE_PROFIT_LOCK` | trigger 1.2R / result 1.05R | 全ペア共通の利益確保 |
| `PAIR_RISK_MULTIPLE_PROFIT_LOCK` | `{}` | ペア個別の上書き。`None`を入れるとそのペアだけ無効 |

2026-08-26 に **RR下限1.4と利益確保を全ペア共通のデフォルトに変更**。
それ以前は AUD_USD のみに適用していた。

**安全設計**：
- RR下限は「その水準に届くセルが1つも無ければ無視」する助言的な仕様。
  したがって下限を上げてもポリシー不在にはならない。
- その副作用として、tierのRRが `trigger_r`（1.2）以下になることがありうる。
  その場合そのtierは利益確保を**適用せず**に走る（例外で落とさない）。
  どのtierに適用されたかは artifact の `risk_multiple_profit_lock_tiers` に記録。
- artifactに記録され、OOSリプレイは記録値を読み戻すので、後から設定表を変えても
  凍結された方針が再現される。

## 完了済み（2026-08-26）

### 条件選定ロジックの改善
- ランキング基準を「合計sum_yen」→「1トレードあたり平均円（avg_yen_per_trade）」に変更。
  従来は取引回数が多いだけの平凡な条件が上位に来ていた。
- 最低トレード数ゲート（30件）を追加。
- train期間内の4四半期安定性ゲート（3期間以上黒字）を追加。TP/LC選定では既にあった
  規律を、条件マイニングにも適用。
- ボンフェローニ補正によるz値診断を追加（ハードゲートにはせず参考値として記録）。
  候補230件なら基準z=3.70。現状の最良条件でもz=2.90で未達＝「まだ断言はできない」。

### 特徴量の追加・改善
- `f_minutes_since_flip`：レジサポ転換からの経過時間。
- `f_line_core_total_strength`：コアピークのみの合計強度（データはあったが未使用だった）。
- `f_line_core_strength_ratio`：コア強度÷全体強度。**AUD_USDでrank1を獲得**
  （`0p6to0p99 & f_distance_a=1to2` → train win74.4%/PF2.78/z2.90）。
  コアと非コアが混在するラインが最も効く、という発見。
- `f_line_relative_strength`：同一イベント内の他ライン中央値との比。絶対値の点質量を回避。
- バケット境界を実測分布に合わせて全面的に見直し（`count2_flip_bucket_report.py`で計測）。
- `PAIR_BUCKET_OVERRIDES` でペアごとにバケット境界を設定可能に。
  現在USD_JPYのみ `f_line_average_strength` を上書き（eq5が63%と偏るため）。

## ペア間で条件が再現しない問題（2026-08-26 判明）

今回の改善は AUD_USD では明確に効いたが、USD_JPY では効かなかった。
両ペアで上位に来る条件が全く別系統：

| | AUD_USD | USD_JPY |
|---|---|---|
| rank1 | `f_line_core_strength_ratio=0p6to0p99 & f_distance_a=1to2`（win74.4%/PF2.78/z2.90） | `f_fc2_second_wick_a=1p00to1p49`（win62.9%/PF2.05/z2.01） |
| 上位の傾向 | ライン構造系（コア強度比率、コアタッチ数） | ローソク形状系（fc2の2本目のヒゲ・押し戻し） |
| HIGH tier OOS | +466（PF1.49） | -283（PF0.75） |

AUD_USD の rank1 だった `f_line_core_strength_ratio` は USD_JPY の上位15に入らない。
「flip_predict の枠組みで安定して勝てる普遍的な条件」はまだ見つかっていない、
というのが現時点の評価。ペアごとに別の条件セットを持つ設計は既にそうなっているが、
「どちらか一方でしか効かない条件」を掴んでいる可能性（＝偶然）は常に疑う必要がある。

## 判明している構造的な制約

- **点質量**：`line_core_count`は1が76%(AUD)/82%(USD_JPY)、`line_average_strength`は
  5.0ちょうどが45%/63%。元データが特定値に集中しているため、バケット境界をどう動かしても
  分割できない。→ 派生指標（比率・相対値）で回避する方向。
- **統計的有意性**：候補を総当たりしている以上、多重比較の問題は避けられない。
  現状どの条件もボンフェローニ補正後の基準を満たしていない。
- **count=2 の定義上の制約**：シグナル自体が2回タッチ済みを意味するため、
  「反転後の初回タッチ（prior_retouch_count=0）」は原理的に存在しない。

## 未着手のアイデア

- エントリータイミング3モード（`FlipWatchEntryConfig`）の閾値0.10A/1.00Aが
  一度も検証されていない。他のパラメータと違いグリッドサーチ対象外のまま。
  → 計画書 `~/.claude/plans/flip-2023-2025-2025-2026-jaunty-cosmos.md` に詳細あり。
- 3条件以上の組み合わせ探索（現在は最大2条件）。
- 連続値の閾値そのものを探索対象にする（現在はバケット境界が固定）。
