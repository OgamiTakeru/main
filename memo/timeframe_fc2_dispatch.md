<!-- 最新更新日時: 2026-09-09 16:13 JST -->
# 時間足別FC2判定（2026-09-09）

- 変更前の保存点: `b66c6ad`。GitHubの`origin/main`へpush済み。
- M30/H1ブレイクは「その時間足の線＋その時間足自身のFC2」。旧M5 FC2方式とは別であり、旧成績を新方式の実績とは扱わない。
- M30は毎時00分06秒・30分06秒、H1は毎時00分06秒を予定時刻にし、境界分の6秒以上30秒未満に最初に回ったループで判断する。M5は5分境界で同様。遅延した古い枠の新規注文は後追いしない。
- 渡すデータの基準時刻は境界T。T+6秒に取得しても、形成中の足やTより後に終了する足はFC2判断に含めない。最新M30/H1確定足の終了時刻がそれぞれのTと一致しなければ新規発行しない。
- 正常な足データ・Peaksは、M30は同じ30分枠、H1は同じ1時間枠内で再利用する。不完全な履歴は正常キャッシュに固定せず、後続の取得で復旧できるようにする。M5戦略が参照するH1の時点情報は、従来どおり各M5判断時刻に合わせて更新する。
- `fAnalysisSchedule.AnalysisRunLedger`で戦略・時間足ごとの処理済み境界を保存。口座・環境・通貨ごとにファイルを分ける。シグナルなしも処理済みとし、同じ足の複数候補は一回の判断にまとめる。
- 記録は注文登録前に保存する。保存後に停止・送信結果不明となっても、その足を自動再発行しない。これは二重送信防止優先の設計であり、「必ず一回約定する」保証ではない。
- `runtime_state/analysis_*.json`は運用状態であり、一時ファイルではない。再起動時も保持する。壊れた記録は無視して上書きせず、新規注文を抑止する。
- 現行ポリシーはM30ブレイクのみ有効のまま。H1への対応は実発注の有効化ではない。M5 flipや既存注文・ポジションの短周期監視も、M30/H1の間隔へ変更しない。
- RR・TP/LC・リスク・注文／保有時間上限は今回変更しない。M30/H1注文も従来どおりTP/LC幅はM5平均幅から計算し、`target_timeframe=M5`を明記する。
- 総当たりのSTOPモードもM30/H1自身のFC2・方向・形状へ変更。結果名に`native_m30_h1_fc2_v2`を付け、旧結果を混ぜない。逆張りlimitの既存M5トリガーは維持する。
- 総当たりは各候補を独立に評価する既存仕様。M30/H1の未約定期限はそれぞれ次の同時間足FC2まで。本番の固定時間上限・ポジション管理と完全に同じ口座リプレイではない。またS5約定探索は既存どおり境界Tからであり、本番T+6秒の遅延再現は今回変更していない。
- 表示専用の旧`resistance_live_monitor.py`は今回の本番経路ではない。新方式の判定確認は`main_exe.py`からの共通アダプタ、または更新したSTOP検証を使う。
- 回帰テストは追加したが実行していない。本番・検証・リプレイ・OANDA通信も実行していない。実施したのは差分レビューと構文の静的解析のみ。
